{
  "name": "bh-market-imo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "tunisia-dynasty-map-integration"
  ],
  "scripts": {
    "dev:client": "npm run start --workspace client",
    "dev:server": "npm run dev --workspace server",
    "dev:map": "npm run dev --workspace tunisia-dynasty-map-integration",
    "build:client": "npm run build --workspace client",
    "build:server": "npm run start --workspace server",
    "build:map": "npm run build --workspace tunisia-dynasty-map-integration",
    "test:client": "npm run test --workspace client -- --watchAll=false",
    "test:server": "npm run test --workspace server"
  }
}
